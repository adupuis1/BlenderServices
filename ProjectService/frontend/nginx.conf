server {
    listen 80;
    root /usr/share/nginx/html;

    # the gateway handles /api/ and /auth/, this container only serves the app
    location / {
        try_files $uri /projects/index.html;
    }
}
