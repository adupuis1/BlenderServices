import { NotBuiltYet } from '../../../shared/ui'

// TODO: PUT the .blend file to the presigned upload_url (MinIO needs CORS for that)
export function UploadPage() {
    return <NotBuiltYet title="Upload .blend" what="uploading the .blend file" />
}
