import { NotBuiltYet } from '../../../shared/ui'

// TODO: GET /api/v1/projects/{id} → project + scan details, rename (PATCH), delete
export function ProjectDetailPage() {
    return <NotBuiltYet title="Project" what="project details, rename and delete" />
}
